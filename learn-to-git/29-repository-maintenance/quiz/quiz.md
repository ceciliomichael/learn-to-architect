# Quiz: Maintain Large and Long-Lived Repositories

1. What does `git count-objects -vH` help you inspect?
2. What broad problem does `git fsck` detect?
3. Why should you avoid immediate pruning in normal work?
4. Does deleting a large file in a new commit remove its old blob from history?
5. Why is removing a committed secret more than a maintenance command?
