# Quiz: Fetch and Remote-Tracking Branches

1. What does `git fetch origin` update?
2. Does fetch normally merge into the current branch?
3. Is `origin/main` located only on the remote server?
4. What does `main..origin/main` select for log?
5. Does upstream tracking continuously synchronize branches?
