# Quiz: Inspect Changes with Diff

1. What does plain `git diff` compare?
2. What does `git diff --staged` compare?
3. What do `+` and `-` mean inside a diff hunk?
4. Why might plain diff be empty while status reports a staged change?
5. Does viewing a diff modify the repository?
