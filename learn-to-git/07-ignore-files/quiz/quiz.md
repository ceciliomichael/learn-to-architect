# Quiz: Ignore Generated and Private Files

1. What does `*.log` match?
2. What does a leading `!` do in an ignore rule?
3. Which command explains why a path is ignored?
4. Does adding a tracked file to `.gitignore` stop tracking it?
5. Why is `.gitignore` not a security tool?
