# Quiz: Read Project History

1. In what order does `git log` normally display commits?
2. What does `HEAD^` usually identify?
3. Which command shows a commit and its patch?
4. What does `--all` change in the graph log command?
5. Does `--follow` mean Git permanently stored a rename?
