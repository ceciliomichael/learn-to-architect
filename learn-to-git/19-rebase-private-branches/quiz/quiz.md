# Quiz: Rebase a Private Branch

1. What does rebase replay?
2. Why do rebased commits get new hashes?
3. Which branch should be current before `git rebase main`?
4. What does `git rebase --abort` do?
5. Why is rebasing a shared branch risky?
