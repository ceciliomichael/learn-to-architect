# Quiz: Automate Local Checks with Hooks

1. When does `commit-msg` run?
2. What does a nonzero hook exit status usually mean for a hook that can reject an operation?
3. Are files in `.git/hooks` cloned automatically?
4. What does `core.hooksPath` change?
5. Why must required policy also run on a server or continuous integration system?
