# Quiz Answers: Repositories and Git's Three Locations

1. It creates the repository's private `.git` data and makes the current project folder a Git repository.
2. It is the visible project files that you read and edit.
3. The index.
4. It copies the file's current working tree content into the staging area.
5. The file was staged and then edited again. The index keeps the earlier content while the working tree has the later content.
6. `git rev-parse --show-toplevel`.
