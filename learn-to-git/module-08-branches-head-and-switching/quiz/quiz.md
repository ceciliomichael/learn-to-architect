# Quiz: Branches, HEAD, and Switching

1. What does a branch name point to?
2. What normally happens to the current branch after a commit?
3. What does `HEAD` refer to during normal branch work?
4. What is the difference between `git branch topic` and `git switch -c topic`?
5. Why can switching change visible files?
