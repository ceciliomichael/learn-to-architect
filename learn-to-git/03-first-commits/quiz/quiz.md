# Quiz: Create Your First Commits

1. Does `git commit` automatically include every saved file change?
2. What does a clean working tree mean?
3. What does `HEAD` identify in this module?
4. Why should you run status after staging and before committing?
5. Which message is more useful: `changes` or `Add setup instructions`? Why?
6. Is a local commit automatically stored on GitHub?
