# Quiz: Work in Two Branches with Worktrees

1. What does each linked worktree have independently?
2. What repository data do linked worktrees share?
3. Can one branch normally be checked out in two worktrees?
4. Does a commit in one worktree require fetch in another linked worktree?
5. Why should you use `git worktree remove` instead of deleting the folder manually?
