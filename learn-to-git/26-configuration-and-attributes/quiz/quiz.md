# Quiz: Configuration, Aliases, and Attributes

1. Which configuration scope affects only one repository?
2. Which options show a value's source and scope?
3. Why should shared automation avoid personal aliases?
4. What broad job does `.gitattributes` perform?
5. How does `.gitattributes` differ from `.gitignore`?
