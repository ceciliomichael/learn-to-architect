# Quiz Answers: Stage the Exact Changes You Want

1. It stages all changes under the current folder, so its scope may include several files and deletions.
2. `git add -p notes.txt`.
3. Stage the displayed hunk.
4. `git restore --staged unwanted.txt`.
5. No. Git stores snapshots and detects likely renames when comparing them.
