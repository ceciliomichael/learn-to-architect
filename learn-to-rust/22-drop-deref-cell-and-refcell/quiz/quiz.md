# Quiz

1. When does Rust normally run `Drop`?
2. How can a value be dropped before the end of its scope?
3. Where does `RefCell` enforce borrowing rules?
4. What happens if those borrowing rules are broken?
5. Are `Cell` and `RefCell` intended for shared use across threads?
