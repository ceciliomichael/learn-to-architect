# Quiz

1. Does a lifetime annotation make a value live longer?
2. What relationship does the return type of `longer` express?
3. Why does a struct that stores a reference need a lifetime parameter?
4. What kind of reference commonly has the `'static` lifetime?
5. What is often simpler than returning a reference to locally created data?
