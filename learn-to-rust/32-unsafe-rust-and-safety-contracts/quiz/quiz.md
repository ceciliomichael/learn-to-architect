# Quiz

1. Does `unsafe` disable all Rust safety checks?
2. Who must uphold the contract of an unsafe operation?
3. What should a `SAFETY` comment describe?
4. Why should unsafe blocks be small?
5. Can passing tests prove that unsafe code has no undefined behavior?
