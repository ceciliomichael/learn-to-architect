# Quiz

1. Are Cargo features additive or mutually exclusive?
2. What does `#[cfg(feature = "json")]` do?
3. Does a workspace turn all members into one package?
4. Why commit `Cargo.lock` for an application?
5. Which feature combinations should CI test?
