# Quiz

1. What promise does a trait implementation make?
2. What does a trait bound tell the compiler?
3. When is a `where` clause helpful?
4. Can return-position `impl Trait` return unrelated concrete types from separate branches?
5. What conflict does the orphan rule prevent?
