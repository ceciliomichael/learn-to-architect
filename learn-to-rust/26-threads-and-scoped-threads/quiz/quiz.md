# Quiz

1. What does `thread::spawn` return?
2. Why does a spawned closure often need `move`?
3. What guarantee makes scoped borrowing safe?
4. What does `join` report if the worker panics?
5. Why should a join handle not be silently discarded?
