# Quiz

1. What does `cargo build --release --locked` prevent?
2. Why is installing a Rust target sometimes insufficient for cross-compilation?
3. What sensitive content must be excluded from a release bundle?
4. Why record checksums and build provenance?
5. When should rollback steps be written?
