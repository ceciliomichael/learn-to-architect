# Quiz

1. What ownership pattern does `Box<T>` provide?
2. Does `Rc::clone` clone the inner value?
3. Why can `Rc<T>` not normally be sent between threads?
4. What problem can a cycle of strong `Rc` references cause?
5. When should `Weak<T>` be used?
