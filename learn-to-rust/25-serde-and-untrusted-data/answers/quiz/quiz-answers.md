# Quiz answers

1. It proves that the input can be represented by the chosen serialized Rust type.
2. It does not prove domain validity, authorization, safety, or trust.
3. It keeps unvalidated external data separate from values trusted by domain behavior.
4. It rejects fields that are not declared by the type.
5. A limit prevents one input from consuming unbounded memory or processing time.
