# Exercises: Slices, str, and String

1. Accept `&str` in a word-count function.
2. Return the first word as a borrowed slice.
3. Print bytes and chars for ASCII and multi-byte text.
4. Sum an integer slice without taking its vector.
5. Mutate every element through `&mut [i32]`.
