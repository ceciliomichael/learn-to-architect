# Quiz: Recoverable Errors with Result

1. What are Result's variants?
2. What does `?` do on Err?
3. Should libraries normally print errors?
4. Why use a typed error enum?
5. Is invalid user input a panic condition?
