# Exercises: Enums, Match, and Patterns

1. Model order states Draft, Paid with receipt, and Cancelled with reason.
2. Match every variant and produce display text.
3. Borrow the enum so owned fields remain usable.
4. Use `if let` for only Paid.
5. Replace two conflicting booleans with one enum.
