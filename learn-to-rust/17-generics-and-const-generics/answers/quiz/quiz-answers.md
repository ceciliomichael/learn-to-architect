# Quiz answers

1. `T` is a type selected for a particular use of the function.
2. `T` could be any type, so only operations known to work for every allowed type are available.
3. It is the array length known at compile time.
4. No. Rust checks every concrete use of the generic code.
5. Use a concrete function when the domain accepts only one type or abstraction would hide the contract.
