# Quiz: Slices, str, and String

1. Which type owns growable UTF-8 text?
2. Which type is preferred for borrowed read-only text?
3. Why is direct string indexing rejected?
4. What units does a string slice range use?
5. Does `&[T]` own the elements?
