# Quiz

1. When should code use `PathBuf` instead of `Path`?
2. Why is a path not always safe to convert directly to `String`?
3. What does buffering improve?
4. Why should untrusted input have a size limit?
5. Why must a critical file replacement strategy be tested per platform?
