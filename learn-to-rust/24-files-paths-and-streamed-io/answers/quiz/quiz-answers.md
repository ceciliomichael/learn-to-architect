# Quiz Answers: Module 24

## 1

Paths have platform-specific representation and may not be valid UTF-8; Path models filesystem paths directly.

## 2

It reduces repeated underlying I/O operations by grouping reads or writes.

## 3

Yes. Files can disappear, permissions can change, and external state is not under your program's exclusive control.

## 4

For known-small text files when loading the whole content is simpler and bounded.

## 5

It makes parsing deterministic, testable, and reusable without requiring filesystem setup.
