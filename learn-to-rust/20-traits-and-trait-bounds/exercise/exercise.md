# Exercises: Module 20

Work locally and explain your choices.

## 1. Trace

For Summary above, which method must Article implement and which behavior can it inherit?

## 2. Repair

Debug-print an unconstrained T, then add only the bound required by that operation.

## 3. Modify

Add Video implementing Summary and pass it through the unchanged print_summary function.

## 4. Build

Define a Validatable trait returning Result<(), String>. Implement it for two domain structs and write validate_all over a slice.

Finish with cargo fmt, cargo clippy, cargo test when tests exist, and cargo check.
