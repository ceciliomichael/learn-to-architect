# Quiz: Compile and Run Your First Program

1. What function starts a binary crate?
2. What does `!` after `println` indicate?
3. Where does Cargo place build artifacts?
4. Which profile enables optimizations by default?
5. Should comments repeat obvious syntax?
