# Quiz: Optional Values with Option

1. What are Option's variants?
2. Does None carry an error reason?
3. What does `as_ref` avoid?
4. When is `and_then` useful?
5. Why avoid unwrap for expected absence?
