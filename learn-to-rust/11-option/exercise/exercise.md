# Exercises: Optional Values with Option

1. Find a product name by ID and return Option.
2. Match Some and None directly.
3. Borrow with `as_deref`.
4. Transform present text length with `map`.
5. Replace a risky `unwrap` with explicit behavior for a missing value.
