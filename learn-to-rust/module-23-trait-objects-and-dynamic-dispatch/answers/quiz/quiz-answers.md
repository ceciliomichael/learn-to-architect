# Quiz answers

1. It chooses the implementation at runtime.
2. It can hold different concrete types that share one trait in a single collection.
3. It hides the concrete type behind the `Notice` contract.
4. An enum is clearer when the set of variants is closed and known.
5. It means the shared behavior may not describe what callers actually need.
