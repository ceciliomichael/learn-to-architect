# Quiz

1. What does an `async fn` return when called?
2. What does `.await` allow the runtime to do?
3. What happens when a `JoinHandle` is dropped without awaiting it?
4. Why must cancellation safety be considered around `.await`?
5. Does async automatically speed up CPU-heavy calculations?
