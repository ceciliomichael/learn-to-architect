# Quiz

1. When does iteration over an `mpsc` receiver finish?
2. What separate jobs do `Arc` and `Mutex` perform?
3. What is mutex poisoning?
4. Why should lock scopes be short?
5. What common rule reduces deadlock risk when several locks are needed?
