# Exercise Solutions: Module 06

Read this only after attempting the exercises.

## 1. Trace

n is the parameter, i32 after the arrow is the return type, and n * n is the returned expression.

## 2. Repair

Remove the semicolon, or use an explicit return expression. The function must actually produce the type promised by its signature.

## 3. Modify

Let describe call area once, store the result, then pass it to is_large. This demonstrates composition without duplicated calculation.

## 4. Build

Many formulas are valid. Keep inputs explicit, calculations in focused functions, and I/O or coordination in main.

A different implementation can also be correct. Compare behavior, clarity, and reasoning instead of only comparing text.
