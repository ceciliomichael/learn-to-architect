# Exercise Solutions: Module 04

Read this only after attempting the exercises.

## 1. Trace

Integer division is 3, remainder is 1, and the Boolean expression is true.

## 2. Repair

Use an explicit conversion such as whole as f64 when appropriate. The representation change is then visible.

## 3. Modify

An if / else if / else chain is the simplest approach at this point.

## 4. Build

Read into a String, trim and parse as f64, match success/failure, then compute c * 9.0 / 5.0 + 32.0.

A different implementation can also be correct. Compare behavior, clarity, and reasoning instead of only comparing text.
