# Quiz: Expressions, Conditions, Loops, and Match Basics

1. Must an `if` condition be boolean?
2. What does a semicolon do to a block's final expression?
3. Is the end of `1..5` included?
4. Can `loop` produce a value?
5. Why must match patterns be exhaustive?
