# Quiz: Functions, Scope, and Tuples

1. Are function parameter types explicit?
2. What happens when the final expression gains a semicolon?
3. What does `()` represent?
4. Can a tuple contain different types?
5. When is a struct clearer than a tuple?
