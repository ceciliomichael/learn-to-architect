# Quiz Answers: Module 13

## 1

Some(0) contains a real value whose value is zero; None contains no value.

## 2

When absence is a legitimate separate state and every ordinary value should retain its normal meaning.

## 3

The data may legitimately be missing, causing a panic instead of controlled behavior.

## 4

When the caller needs to know why an operation failed rather than only that no value was produced.

## 5

No. It makes absence explicit, but the program still needs domain-specific behavior for that state.
