# Quiz answers

1. Behavior, errors, panics, thread guarantees, features, and data formats can all be contracts.
2. `cargo test` runs documentation tests as well as other tests.
3. It forces construction through validation and preserves the type's guarantees.
4. It requires a fallback arm because future variants may be added.
5. Provide a reason, replacement, and practical migration path.
