# Exercises: Recoverable Errors with Result

1. Parse and validate a positive quantity.
2. Define an error enum for empty, invalid, and zero input.
3. Add a `message` method that gives each variant friendly text.
4. Propagate parsing with `?` after converting its error into your enum.
5. Report the error once in main without panic or advanced error traits.
