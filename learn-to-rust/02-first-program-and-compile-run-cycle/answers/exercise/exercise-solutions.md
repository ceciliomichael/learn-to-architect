# Exercise Solutions: Module 02

Read this only after attempting the exercises.

## 1. Trace

cargo check performs compiler analysis without producing the normal runnable artifact. cargo build produces build artifacts. cargo run ensures the build is current and then starts the executable.

## 2. Repair

Restore the semicolon after the first statement. The parser needs a valid boundary between the statements.

## 3. Modify

Four printing statements are enough. No abstraction is needed yet.

## 4. Build

A single main function with five clear lines is appropriate. The point is the development pipeline, not advanced syntax.

A different implementation can also be correct. Compare behavior, clarity, and reasoning instead of only comparing text.
