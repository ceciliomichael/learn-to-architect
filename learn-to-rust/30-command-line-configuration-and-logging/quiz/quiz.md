# Quiz

1. Why might a program choose `args_os` instead of `args`?
2. Where should normal output and diagnostics go?
3. Why convert raw settings into a typed configuration?
4. Should environment variables be trusted without validation?
5. What information should logs avoid?
