# Quiz

1. What can a closure use that an ordinary function cannot capture automatically?
2. What does `move` do to captured values?
3. Why are `map` and `filter` called lazy?
4. Which method consumes a collection: `iter` or `into_iter`?
5. When is a loop clearer than an iterator chain?
