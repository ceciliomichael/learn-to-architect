# Quiz answers

1. It can capture values from the surrounding scope.
2. It makes the closure take ownership of the captured values it uses.
3. They describe work but do not process items until an iterator consumer runs.
4. `into_iter` consumes it when called on an owned collection.
5. Prefer a loop when several branches, side effects, or errors make the chain hard to read.
