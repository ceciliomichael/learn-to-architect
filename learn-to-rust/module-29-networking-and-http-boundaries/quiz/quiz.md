# Quiz

1. Does TCP preserve application message boundaries?
2. Why should an HTTP client usually be reused?
3. Why set an explicit request timeout?
4. Must decoded JSON from a successful HTTPS request still be validated?
5. Why can retrying a request be dangerous?
