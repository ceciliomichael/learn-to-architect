# Quiz

1. What does `T` represent in `fn first<T>`?
2. Why can a generic function not call every possible method on `T`?
3. What does `const N: usize` represent for an array?
4. Do generics remove Rust's compile-time type checks?
5. When is a concrete function clearer than a generic one?
