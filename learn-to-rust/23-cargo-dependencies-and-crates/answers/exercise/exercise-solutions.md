# Exercise Solutions: Module 23

Attempt the exercises before reading.

## 1. Trace

Dependencies have their own dependencies. Those transitive packages become part of the resolved build graph.

## 2. Repair

Removing the direct crate also removes transitive packages that no remaining dependency needs. One manifest entry can represent a much larger code and trust surface.

## 3. Modify

User-perceived grapheme clusters can combine multiple Unicode scalar values, so byte length and chars iteration answer different questions.

## 4. Build

Keep the review factual. If a maintenance or license fact cannot be verified from authoritative material, mark it unknown rather than inventing it.

Equivalent implementations can be correct when they satisfy the same behavior and constraints.
