# Quiz: Browser Runtime, Project Setup, and Generated JavaScript

1. What does `vanilla-ts` mean in the Vite creation command?
2. Why should the project be opened through the development server instead of directly with `file://`?
3. What roles do Node.js, Vite, TypeScript, and the browser each have?
4. Why does a typed `querySelector` still require a null check?
5. How are `src`, `node_modules`, and `dist` different?
