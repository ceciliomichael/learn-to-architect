# Exercise: Timers, Animation Frames, and Observer APIs

1. Recreate the complete example in a disposable browser project.
2. Change its data and visible text while preserving its boundary contract.
3. Test success, invalid or missing input, and cleanup or recovery where applicable.
4. Inspect browser evidence in the DOM, Console, Network, Storage, Performance, or Accessibility tools.
5. Explain which code owns the resource and which values remain untrusted.

## Success check

Strict type checking passes, browser behavior is keyboard accessible, and failure never silently becomes trusted state.

