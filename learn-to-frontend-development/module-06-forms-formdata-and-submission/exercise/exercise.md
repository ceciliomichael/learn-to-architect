# Exercise: Forms, FormData, and Submission

1. Recreate the complete example in a disposable browser project.
2. Replace its example names and values while preserving the types and browser contract.
3. Exercise the normal path, missing or invalid input, and cleanup or cancellation path where applicable.
4. Inspect the DOM, Console, Network, Storage, or Accessibility panel that provides evidence for this module.
5. Explain which boundary remains untrusted and which code owns recovery.

## Success check

The strict type check passes without assertions that hide missing runtime evidence, and the browser behavior remains accessible and recoverable.

