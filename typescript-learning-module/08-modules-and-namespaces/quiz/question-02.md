# Question 02: Named vs Default Exports

What is the difference between a named export and a default export?

```typescript
// File: utils.ts
export const PI = 3.14159;               // Named export
export function square(n: number) { ... } // Named export
export default class MathHelper { ... }   // Default export
```

1. Show the exact import syntax for importing `PI`, `square`, and `MathHelper` from `utils.ts`.
2. Can you rename a named import? Can you rename a default import? Show the syntax for both.
3. Can a file have multiple default exports? Why or why not?

## ANSWER HERE

```typescript
// Write the import statements here
```

> **Can you rename a named import? Syntax:**

> **Can you rename a default import? Syntax:**

> **Can a file have multiple default exports?**
