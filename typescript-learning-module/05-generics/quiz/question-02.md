# Question 02: Generic Constraints

This function has a bug:

```typescript
function getLength<T>(item: T): number {
  return item.length; // TypeScript gives an error here.
}
```

1. Why does TypeScript give an error on `item.length`?
2. How do you fix it using the `extends` keyword?
3. After the fix, what types can you pass to the function? What types would still be rejected?

## ANSWER HERE

> **Why TypeScript errors on `item.length`:**

> **The fix using extends:**

```typescript
// Write the corrected function here:
function getLength


```

> **What types are now accepted:**

> **What types are still rejected:**
