# Module 10 Quiz: Architectural Rules & Clean Code

Write your answers inside the **ANSWER HERE** blocks below.

---

### Question 1: Single Responsibility Principle (SRP)
Why is co-locating HTTP API calls directly inside UI components considered an architectural violation? How does separating data fetching into dedicated service modules improve codebase maintainability?

#### ✍️ ANSWER HERE:
> Explanation: 

---

### Question 2: Circular Dependencies
What is a circular dependency between two TypeScript files (`A.ts` imports `B.ts`, and `B.ts` imports `A.ts`), and why can it cause runtime initialization bugs or bundler failures?

#### ✍️ ANSWER HERE:
> Explanation: 

---

### Question 3: The 300-Line Limit
When a file approaches the 300-line threshold, what architectural anti-patterns does this usually indicate regarding the file's responsibilities?

#### ✍️ ANSWER HERE:
> Explanation: 
