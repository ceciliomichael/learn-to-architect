# Challenge 02: Plan a Project from Scratch

Design the folder and file structure for a small application before writing a single line of code.

## The Scenario

You are building a task management application. Users can
- Create a task (title is required, cannot be empty).
- Mark a task as completed (cannot complete an already completed task).
- View all tasks.

A task has: `id: string`, `title: string`, `isCompleted: boolean`, `createdAt: Date`.

## Your Tasks

1. Draw the complete folder structure in a code block. Include every file you would create, with a one-line responsibility comment next to each.

2. Write the complete content of `types/index.ts`.

3. Write the complete content of `services/taskService.ts` (business logic: create, complete, getAll  -  with validation).

4. Write a short `index.ts` entry point that calls each service function and logs results.

## ANSWER HERE

**Folder structure:**

```
task-manager/
├── ...
```

**types/index.ts:**
```typescript

```

**services/taskService.ts:**
```typescript

```

**index.ts:**
```typescript

```
