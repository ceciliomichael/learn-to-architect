# Question 04: `extends` vs `implements`

What is the difference between `extends` (inheritance) and `implements`? A class can implement multiple interfaces but can only extend one class; why is this?

## ANSWER HERE

> **Difference between `extends` and `implements`:**

> **Why a class can implement multiple interfaces but only extend a single class:**

> **When should you choose an interface contract over class inheritance?**
