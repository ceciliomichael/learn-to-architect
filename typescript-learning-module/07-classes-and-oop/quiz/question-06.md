# Question 06: Getters, Setters, and Static Members

How do TypeScript accessors (`get` and `set`) differ from standard class methods? Furthermore, how do `static` class members differ from instance members in computer memory?

1. What is the architectural advantage of exposing a private property via a `get` / `set` pair instead of making the property `public` or writing explicit methods like `getTemperature()` and `setTemperature(val)`?
2. When you declare a method or property as `static`, where is it stored in memory? Can you access `this.instanceProperty` inside a static method? Why or why not?
3. Give an enterprise backend use case where static members are essential.

## ANSWER HERE

> **Architectural advantage of `get` / `set` accessors:**

> **Memory storage of `static` members and the behavior of `this`:**

> **Real-world enterprise use case for static members:**
