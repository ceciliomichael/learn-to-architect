# Question 01: Access Modifiers at Runtime

You mark a class property `private balance: number`. In TypeScript, accessing `account.balance` from outside the class is a compile error.

**But does the `private` keyword actually protect the property at runtime in the compiled JavaScript?**

1. What does TypeScript's `private` keyword do at compile time?
2. What happens to `private` when the code is compiled to JavaScript? Can you still access `balance` from outside at runtime in plain JavaScript?
3. If you need true, unbreakable runtime privacy, what syntax do you use instead?

## ANSWER HERE

> **What `private` does at compile time:**

> **What happens at runtime after compilation:**

> **The syntax for true runtime privacy:**
