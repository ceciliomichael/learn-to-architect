# Challenge 04: Rest Parameters

Practice functions that accept any number of arguments.

## Your Tasks

**Function 1: `calculateTotal`**
- Uses a rest parameter `...numbers: number[]`
- Sums all numbers using a `for...of` loop
- Returns the total as a `number`

**Function 2: `joinWords`**
- Uses a rest parameter `...words: string[]`
- Returns all words joined into one string with a single space between each word
- Hint: You can use a `for...of` loop to build the string manually, or check if you know any built-in method that joins array items.

Test `calculateTotal` with at least 5 different numbers. Test `joinWords` with at least 4 words. Log all results.

## ANSWER HERE

```typescript
// Write calculateTotal and joinWords here
```
