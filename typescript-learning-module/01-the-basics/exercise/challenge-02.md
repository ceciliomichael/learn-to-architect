# Challenge 02: Build a Typed Inventory

You are building a simple game inventory system using typed arrays.

## Your Tasks

1. Declare a `string[]` array called `itemNames` with at least 3 item names.
2. Declare a `number[]` array called `itemDamage` with at least 3 damage values.
3. Declare a `boolean[]` array called `isEquipped` with at least 3 true/false values (matching the same items).
4. Use `console.log` to print:
   - The first item name (index 0).
   - The total count of items using `.length`.
   - The second item name converted to uppercase using `.toUpperCase()`.
5. Add a fourth item to `itemNames` using `.push()` and log the updated length.

## ANSWER HERE

```typescript
// Write your typed arrays and console.log statements here:


```

