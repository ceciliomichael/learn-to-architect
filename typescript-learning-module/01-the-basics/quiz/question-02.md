# Question 02: const and Arrays

Look at this code
```typescript
const scores: number[] = [90, 85];
scores.push(100);
```

Does TypeScript throw an error on the `.push()` line?

Explain why or why not. In your answer, specifically mention what `const` actually protects and what it does NOT protect.

---

## ANSWER HERE

> **Does it error? (Yes / No):**

> **Explanation:**
