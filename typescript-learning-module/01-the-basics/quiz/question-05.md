# Question 05: String Methods

You have the string `"10,20,30"`.

1. Write a line of code using `.split()` that turns it into an array of three strings: `["10", "20", "30"]`.
2. Write a second line that accesses the second item in that array (the string `"20"`) using its index.
3. Write a third line that logs that second item converted to all capital letters. (Hint: `"20".toUpperCase()` returns `"20"` since there are no letters, but the concept is identical; practice the method call to build muscle memory.)
4. Bonus: Use `parseInt()` to convert the second item into an actual number and log it.

---

## ANSWER HERE

```typescript
// Write your .split(), index access, and method calls here
```
