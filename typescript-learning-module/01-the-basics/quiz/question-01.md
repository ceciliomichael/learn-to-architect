# Question 01: What Happens to Types at Runtime?

When TypeScript compiles your `.ts` file into a `.js` file, what happens to all the type annotations like `: string` and `: number[]`?

**Options:**

A) They are converted into runtime `typeof` checks automatically.

B) They are completely removed and do not exist in the running JavaScript.

C) They are kept in the JavaScript output file as comments.

D) Node.js reads them at startup before executing the program.

---

## ANSWER HERE

> **My choice:**

> **Explanation (in your own words):**
