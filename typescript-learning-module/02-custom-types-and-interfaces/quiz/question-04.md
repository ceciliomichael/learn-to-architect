# Question 04: type vs interface Capabilities

Both `type` aliases and `interface` declarations can describe object shapes. But there is one category of type that only `type` can express and `interface` cannot.

1. What is that category? Give a concrete example of a `type` declaration that an `interface` cannot replicate.
2. Name one thing `interface` can do that `type` cannot.

## ANSWER HERE

> **What only `type` can do (with example):**

> **What only `interface` can do:**
