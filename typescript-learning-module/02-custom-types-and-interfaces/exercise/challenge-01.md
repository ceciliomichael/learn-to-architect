# Challenge 01: Design a Game Character Blueprint

You are building the type system for a role-playing game.

## Your Tasks

1. Create an `interface` called `GameCharacter` with these exact properties:
   - `id`: a `readonly` string (cannot be changed after creation).
   - `name`: a string.
   - `health`: a number.
   - `level`: a number.
   - `guild`: an optional string (some characters may not belong to a guild).
   - `skills`: an array of strings.

2. Create a variable `warrior` using the `GameCharacter` interface. Give it a guild.

3. Create a variable `rogue` using the `GameCharacter` interface. Leave the `guild` out entirely.

4. After creating `warrior`, write a commented-out line that tries to change `warrior.id`. Write the TypeScript error in the comment.

## ANSWER HERE

```typescript
// Write your interface and objects here:


```
