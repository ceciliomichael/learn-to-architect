# Question 04: `Record` Resolved Type

## Question

Given the following `Record` type:

```typescript
type AdminMap = Record<"admin" | "editor", boolean>;
```

1. What does `AdminMap` resolve to?
2. Write out the **exact equivalent interface** that has an identical shape.

---

## Answer

> ANSWER HERE
