# Question 05: Combining Utility Types

## Question

You have the following `User` interface
```typescript
interface User {
  id: string;
  username: string;
  email: string;
  passwordHash: string;
  createdAt: Date;
}
```

You need a new type that
1. **Removes** the `id` and `createdAt` fields.
2. Makes **everything else** optional.

Write the **single combined utility type expression** that achieves this. Do not define a new interface.

---

## Answer

```typescript
// ANSWER HERE
```
