# Exercise Solution: Online Compiler and Local Toolchain Choices

```c
#include <stdio.h>

int main(void)
{
    puts("Learning one step at a time.");
    return 0;
}
```

## Why this solution works

The program compiles without warnings and prints exactly one line. The code also keeps the lesson's types, boundaries, and failure paths visible instead of depending on an unstated assumption.

Compile it with the lesson command and compare actual output with the success check. A different solution is valid when it satisfies every stated contract without warnings.

