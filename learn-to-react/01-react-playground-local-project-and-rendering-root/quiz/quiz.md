# Quiz: React Playground, Local Project, and Rendering Root

1. What does the `react-ts` template add compared with a plain TypeScript project?
2. Why must `npm install` run after the Vite project is created?
3. Which file contains the DOM element with `id="root"`?
4. Why does `src/main.tsx` check the root element for `null`?
5. What different responsibilities belong to `src/main.tsx` and `src/App.tsx`?
