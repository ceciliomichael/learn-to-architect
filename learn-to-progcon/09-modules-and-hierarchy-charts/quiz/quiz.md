# Module 09 Quiz

## Question 1

In this course, what is a module?

A. A programming language keyword that always means a file  
B. A named group of steps that does one job  
C. A flowchart symbol used only for decisions  
D. A trace table column for constants  

## Question 2

What is the main purpose of a hierarchy chart?

A. To show every arithmetic formula in detail  
B. To list every variable and its data type  
C. To show which modules exist and who calls whom  
D. To replace pseudocode completely  

## Question 3

Read this design:

```text
Module Main
    Call GetTemperature
    Call ConvertToFahrenheit
    Call DisplayResult
End Module
```

If `GetTemperature` displays "Enter Celsius:" and then reads a value, what happens immediately after `GetTemperature` finishes?

A. The program always ends.  
B. Control returns to Main, which then calls `ConvertToFahrenheit`.  
C. `DisplayResult` runs before conversion.  
D. Main restarts from the first line.  

## Question 4

Which module name best matches the single-job guideline?

A. `DoThings`  
B. `Process`  
C. `CalculateAttendancePercent`  
D. `Module2`  

## Question 5

A module named `DisplayAverage` needs an already calculated average grade. In plain design language, what should the design make clear?

A. That DisplayAverage invents the average with no input  
B. That DisplayAverage receives the average as data it needs (a parameter)  
C. That DisplayAverage must also ask for every original score again  
D. That hierarchy charts should include the average formula  

Then read the [quiz answers](../answers/quiz/quiz-answers.md).
