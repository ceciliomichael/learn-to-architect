# Module 10 Quiz

## Question 1

Which list correctly names the three basic control structures used in structured design?

A. Input, output, storage  
B. Sequence, selection, loop  
C. Module, variable, constant  
D. Hierarchy, trace, arithmetic  

## Question 2

What is spaghetti code?

A. Code that only uses sequence  
B. Logic that is hard to follow because control jumps around without clear structure  
C. A hierarchy chart with more than three modules  
D. Any program that displays a message  

## Question 3

Which fragment is mainly a loop?

A. `Set total = price + tax`  
B. `If age >= 18 then Display "Adult"`  
C. `While count <= 10 Display count; Set count = count + 1`  
D. `Call DisplayWelcome`  

## Question 4

Why does structured design prefer one entry point and one exit point for a control structure?

A. Because programs may never use modules  
B. Because it makes the path of control easier to read, test, and change  
C. Because selection is not allowed to have two branches  
D. Because hierarchy charts cannot show more than one module  

## Question 5

A messy design uses many GoTo labels for pass, fail, and invalid score messages. What is the best first restructuring step taught in this module?

A. Add more labels so every line has a name  
B. Replace the jumps with a clear selection structure and, if useful, modules  
C. Remove all Display statements  
D. Convert every step into a loop  

Then read the [quiz answers](../answers/quiz/quiz-answers.md).
