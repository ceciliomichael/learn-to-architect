# Module 19 Quiz Answers

## Answer 1

Set the accumulator to `0` before the loop begins (for a sum).

## Answer 2

Total is `19`. Average is `19 / 3`, which is approximately `6.333...`.

## Answer 3

If every value is negative, starting at `0` can never be beaten by a real maximum that is below zero. Starting at the first element uses a real value from the data.

## Answer 4

**D.** With 6 valid values, indexes are `0` through `5`.

## Answer 5

```text
SET minimum = nums[0]
FOR index FROM 1 TO 3 STEP 1
    IF nums[index] < minimum THEN
        SET minimum = nums[index]
    END IF
END FOR
```
