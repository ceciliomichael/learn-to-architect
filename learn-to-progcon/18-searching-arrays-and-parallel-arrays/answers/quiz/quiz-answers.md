# Module 18 Quiz Answers

## Answer 1

A linear search checks array elements one by one from the beginning until it finds the target value or reaches the end without a match.

## Answer 2

`Cal`

Id `9` is at index `2`, so the parallel name is `names[2]`.

## Answer 3

Each index is treated as one paired record. If the arrays have different lengths or a different order, an index can join the wrong values or point past the end of one array.

## Answer 4

**B.** Report that the target was not found.

## Answer 5

New products can be added as new array elements instead of new nested decisions. A search plus index lookup stays the same shape even when the list grows.
