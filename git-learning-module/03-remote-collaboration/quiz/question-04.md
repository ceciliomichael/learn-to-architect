# Question 04

What sequence of internal operations does the `git clone` command execute under the hood?

A) It only downloads a single file from the internet.
B) It deletes the remote repository after transferring it to your local machine.
C) It creates a folder, initializes a local Git repository, configures the 'origin' remote, and downloads the full project history.
D) It pushes your local code to a new server.
