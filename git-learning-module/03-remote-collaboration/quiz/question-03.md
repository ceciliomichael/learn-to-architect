# Question 03

What is the fundamental architectural difference between `git fetch` and `git pull`?

A) `fetch` downloads data without integrating it into your current branch, while `pull` downloads and then integrates according to your merge/rebase configuration.
B) `pull` only downloads specific files, while `fetch` downloads the entire repository.
C) `fetch` is used exclusively for remote branches, while `pull` is used for local branches.
D) They are completely identical commands and do exactly the same thing.
