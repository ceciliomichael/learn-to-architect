# Question 02

When executing the command `git push -u origin main`, what is the architectural purpose of the `-u` flag?

A) It updates your local files to match the remote server.
B) It forces the remote server to undo its last commit.
C) It establishes a permanent tracking relationship (upstream) between your local branch and the remote branch.
D) It un-stages files before pushing them to the server.
