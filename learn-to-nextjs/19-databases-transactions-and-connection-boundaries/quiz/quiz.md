# Quiz: Databases, Transactions, and Connection Boundaries

1. Why is `postgres` installed as a production dependency?
2. What protection does `import "server-only"` add?
3. Why are values inside the SQL tagged template not SQL string concatenation?
4. What happens when the destination update fails inside `sql.begin`?
5. Why is the nonnegative database constraint necessary when the application already checks the amount?
