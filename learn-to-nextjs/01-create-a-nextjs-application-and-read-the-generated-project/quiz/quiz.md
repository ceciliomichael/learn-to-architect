# Quiz: Create a Next.js Application and Read the Generated Project

1. What does `npx create-next-app@latest nextjs-course --use-npm` do?
2. Which generator choice makes the `app` directory control routes?
3. What roles do `app/page.tsx` and `app/layout.tsx` have?
4. Why should you avoid editing `.next` and `node_modules`?
5. What additional evidence does `npm run build` provide after `npm run dev` works?
