# Quiz: Filter Rows with Comparisons

1. What does `WHERE` do for a `SELECT` query?
2. Is `BETWEEN 10 AND 20` inclusive?
3. What does `%` mean in a `LIKE` pattern?
4. Why should mixed `AND` and `OR` conditions use parentheses?
5. Does filtering a result delete nonmatching rows?
