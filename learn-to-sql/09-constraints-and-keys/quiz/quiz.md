# Quiz: Protect Data with Constraints and Keys

1. What does a primary key identify?
2. What does a foreign key require?
3. Why combine `NOT NULL` with a range `CHECK`?
4. Does a default replace an explicitly supplied `NULL`?
5. What must SQLite connections do for foreign-key enforcement?
