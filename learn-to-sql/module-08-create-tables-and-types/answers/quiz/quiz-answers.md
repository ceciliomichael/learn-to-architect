# Quiz Answers: Create Tables and Choose SQLite Types

1. `NULL`, `INTEGER`, `REAL`, `TEXT`, and `BLOB`.
2. A column's preference for converting inserted values to a storage class.
3. No.
4. `INT`, `INTEGER`, `REAL`, `TEXT`, `BLOB`, and `ANY`.
5. It can represent fixed minor units exactly without floating-point fractions.
