# Quiz: Create Tables and Choose SQLite Types

1. What are SQLite's five storage classes?
2. What is column affinity?
3. Does `VARCHAR(20)` enforce a 20-character limit in SQLite?
4. Which declared types can a strict table use?
5. Why might cents be stored as an integer?
