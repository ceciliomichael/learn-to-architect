# Quiz: Meet Databases, SQL, and SQLite

1. What does one row represent in the course `books` table?
2. What is the purpose of a column in a database table?
3. Is `.tables` an SQL query? How does a dot command differ from an SQL statement?
4. What character ends an SQL statement in standard SQL databases?
5. What does `SELECT * FROM books;` request?
6. Where does SQLite store the `library.db` database?
7. What is the key architectural difference between SQLite (embedded) and MySQL/PostgreSQL (client-server)?
8. At what point in this course do we transition from core query logic in SQLite to client-server database topics?

