# Quiz: Update and Delete Rows Safely

1. What should happen before an update or delete?
2. What happens if an update has no `WHERE`?
3. Why include an expected state with a primary key in some deletes?
4. What does `ROLLBACK` do?
5. What does SQLite `changes()` report?
