# Quiz: Change Schemas with Migrations

1. Why are migrations ordered and recorded?
2. Why can adding `NOT NULL` fail for existing rows?
3. What must a rebuild preserve besides columns and rows?
4. Should an applied migration be edited?
5. Why is every migration not truly reversible?
