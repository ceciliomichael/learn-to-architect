# Quiz Answers: Change Schemas with Migrations

1. Every database must reach the same schema through the same known sequence.
2. Old rows have no value for the new required column.
3. Indexes, triggers, views, constraints, foreign keys, and expected identifiers.
4. No. Add a new migration.
5. Dropped or transformed data cannot always be reconstructed from schema alone.
