# Quiz Answers: Filter Rows with Comparisons

1. It keeps source rows whose condition evaluates to true.
2. Yes. Both endpoints are included.
3. Zero or more characters.
4. Parentheses state the intended grouping clearly and prevent precedence from changing the meaning.
5. No. A filtered `SELECT` only reads a chosen result.
