# Quiz: Insert Rows

1. Why should an insert name target columns?
2. How does SQLite generate an omitted `INTEGER PRIMARY KEY`?
3. What does `RETURNING` provide?
4. Is a multi-row insert statement partly applied if one row violates a constraint?
5. Why should conflict errors not be ignored automatically?
