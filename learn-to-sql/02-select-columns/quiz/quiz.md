# Quiz: Select Columns and Name Results

1. Does the order of items in the `SELECT` list affect output column order?
2. Does a column alias rename the stored table column?
3. Why are explicit columns usually clearer than `*`?
4. Why does `SELECT author FROM books;` repeat some names?
5. Is result row order guaranteed without `ORDER BY`?
