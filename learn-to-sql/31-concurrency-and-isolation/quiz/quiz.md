# Quiz: Concurrency, Locking, and Isolation

1. How many simultaneous SQLite writers are allowed?
2. What snapshot does PostgreSQL Read Committed use?
3. Can PostgreSQL Serializable abort a correct transaction?
4. What must a serialization retry repeat?
5. Does SQLite support `SELECT FOR UPDATE`?
