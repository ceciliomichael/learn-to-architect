# Quiz: Parameterized SQL, Roles, and Least Privilege

1. What makes concatenated user input dangerous?
2. What do parameters separate?
3. Can a value parameter replace a column name?
4. Why should a runtime role not own tables?
5. Does `ALTER DEFAULT PRIVILEGES` change existing objects?
