# Quiz: Understand NULL and Three-Valued Logic

1. Is `NULL` the same as zero?
2. Which operator tests for a missing value?
3. What does `WHERE` do with an unknown condition?
4. Why does `column <> 5` not include a row where `column` is `NULL`?
5. Does `COALESCE` update stored data?
