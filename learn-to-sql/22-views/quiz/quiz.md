# Quiz: Create and Use Views

1. Does an ordinary SQLite view store copied result rows?
2. Why use explicit output names?
3. Are SQLite views directly writable by default?
4. Does dropping a view delete base rows?
5. Is a view automatically a security boundary?
