# Quiz: Calculate Aggregate Results

1. What does `COUNT(*)` count?
2. What does `COUNT(column)` exclude?
3. Does `AVG` include `NULL` as zero?
4. What normally does `SUM` return for no nonmissing inputs?
5. Why is a bare detail column beside a whole-table average unclear?
