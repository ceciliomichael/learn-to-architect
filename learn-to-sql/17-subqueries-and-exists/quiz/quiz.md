# Quiz: Use Subqueries and EXISTS

1. What shape should a scalar subquery return?
2. What does `EXISTS` test?
3. Why does its inner list often select 1?
4. What can `NULL` do to `NOT IN`?
5. What does a subquery in `FROM` act as?
