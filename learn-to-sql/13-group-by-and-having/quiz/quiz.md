# Quiz: Group Rows and Filter Groups

1. What determines one result group?
2. Does `WHERE` run before grouping in the reasoning model?
3. Which clause filters aggregate results?
4. How are `NULL` grouping values treated?
5. Why avoid SQLite's permissive bare columns?
