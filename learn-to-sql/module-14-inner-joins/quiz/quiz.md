# Quiz: Join Matching Rows

1. What does an inner join keep?
2. Why qualify `name` in this schema?
3. What should an `ON` clause describe?
4. What is a Cartesian product?
5. Why should `DISTINCT` not be the first fix for duplicate join rows?
