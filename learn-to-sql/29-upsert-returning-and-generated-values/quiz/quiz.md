# Quiz: Upsert, RETURNING, and Generated Values

1. What does `excluded` represent?
2. Why name a conflict target?
3. Does `RETURNING` make SQLite and PostgreSQL behavior identical?
4. Can an SQLite generated expression contain a subquery?
5. How do virtual and stored generated columns differ?
