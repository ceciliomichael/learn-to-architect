# Quiz: Use Triggers Carefully

1. Which row images exist during update?
2. What does `WHEN` do?
3. Is a trigger audit committed separately from its update?
4. Why can triggers be hard to maintain?
5. Does SQLite `RETURNING` report rows changed only by triggers?
