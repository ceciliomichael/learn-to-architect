# Quiz: Work with Dates, Times, and Portability

1. Does SQLite have a dedicated date storage class?
2. What three time representations does SQLite commonly use?
3. What time zone does SQLite `now` use?
4. Why is a calendar date not always an instant?
5. Does PostgreSQL `timestamp with time zone` preserve the input zone name?
