# Quiz: Build Expressions and Decisions

1. Does a calculated `SELECT` expression update stored data?
2. What is SQLite's result for `5 / 2` when both values are integers?
3. What does `||` do?
4. How does searched `CASE` choose among several true-looking ranges?
5. What result does `CASE` use when no branch is true and there is no `ELSE`?
