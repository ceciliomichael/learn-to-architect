# Quiz: Use String and Numeric Functions

1. What does a scalar function return for each input row?
2. At what position does SQLite `substr` begin counting text?
3. Which function removes surrounding spaces?
4. In `upper(trim(value))`, which function runs first?
5. Why should function behavior be checked for the target database?
