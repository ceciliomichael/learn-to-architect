# Quiz Answers: Combine Result Sets

1. Column count and compatible meaning and value types by position.
2. `UNION` removes duplicate rows. `UNION ALL` keeps them.
3. Rows found in both results.
4. Yes. It keeps first-result rows absent from the second.
5. Set operations stack rows; joins combine columns from related rows.
