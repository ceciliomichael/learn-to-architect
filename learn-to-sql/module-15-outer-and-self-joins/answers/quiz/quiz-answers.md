# Quiz Answers: Keep Unmatched Rows with Outer Joins

1. The left input.
2. `NULL`.
3. The missing-match values are `NULL`, so ordinary conditions are not true.
4. It ignores the synthetic missing child row while `COUNT(*)` counts it.
5. The same column names need distinct relationship roles.
