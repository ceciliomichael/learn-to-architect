# Quiz: Keep Unmatched Rows with Outer Joins

1. Which input does a left join always preserve?
2. What appears in right-side columns when no row matches?
3. Why can a right-side `WHERE` condition remove unmatched rows?
4. Why use `COUNT(child_id)` instead of `COUNT(*)` for child counts?
5. Why does a self join require aliases?
