# Quiz: Sort, Remove Duplicates, and Limit Results

1. What guarantees result row order?
2. Why add a unique final sort key?
3. What does multi-column `DISTINCT` compare?
4. Why is `LIMIT 3` not a top-three query by itself?
5. When is `OFFSET` applied in the lesson's reasoning order?
