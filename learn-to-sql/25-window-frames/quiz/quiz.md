# Quiz: Control Window Frames

1. How does a frame differ from a partition?
2. What does `2 PRECEDING` mean in a `ROWS` frame?
3. What are peers?
4. Why can an omitted frame surprise running totals?
5. What frame ends at the partition's last row?
