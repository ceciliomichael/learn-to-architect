# Quiz: Combine Result Sets

1. What must match across set-operation queries?
2. What is the difference between `UNION` and `UNION ALL`?
3. What does `INTERSECT` keep?
4. Is `EXCEPT` direction-sensitive?
5. How do set operations differ from joins?
