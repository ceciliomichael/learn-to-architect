# Module 09 Quiz Answers

1. **Answer: B**
   - **Reasoning**: `INNER JOIN` matches rows based on the `ON` predicate and includes only records that exist in both tables.

2. **Answer: C**
   - **Reasoning**: In a `LEFT JOIN`, when a row from the left table has no matching record in the right table, SQLite populates all right-table columns with `NULL`.
