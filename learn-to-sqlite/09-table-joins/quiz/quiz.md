# Module 09 Quiz

1. Which join type returns only rows that have matching values in both tables?
   - A) `LEFT JOIN`
   - B) `INNER JOIN`
   - C) `FULL JOIN`
   - D) `CROSS JOIN`

2. What is the value of unmatched right-side fields in a `LEFT JOIN`?
   - A) `0`
   - B) Empty String `''`
   - C) `NULL`
   - D) Error
