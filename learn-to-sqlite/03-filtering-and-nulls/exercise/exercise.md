# Module 03 Exercise

1. Open SQLite and work with the `inventory` table created in Module 03.
2. Write a query to select all items where:
   - Category is `'Electronics'` OR price is less than `20.00`
   - AND `discount_price` is NOT NULL.
3. Display `item_name`, `price`, and `discount_price`.
