# Module 03 Quiz

1. What does the expression `NULL = NULL` evaluate to in SQL?
   - A) `TRUE`
   - B) `FALSE`
   - C) `UNKNOWN`
   - D) `0`

2. Which function returns the first non-NULL argument passed to it?
   - A) `NULLIF()`
   - B) `IFNULL()`
   - C) `COALESCE()`
   - D) `CONCAT()`
