# Module 13 Quiz

1. Does a standard SQL View duplicate and store table rows on disk?
   - A) Yes, views create duplicate disk copies.
   - B) No, a view is a dynamic virtual table that runs its query on demand.
   - C) Only when WAL mode is enabled.
   - D) Only when indexed.

2. Which pseudo-record is available inside an `AFTER DELETE` trigger?
   - A) `NEW`
   - B) `OLD`
   - C) `DELETED`
   - D) `CURRENT`
