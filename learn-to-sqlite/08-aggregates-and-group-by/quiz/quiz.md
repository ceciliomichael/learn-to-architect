# Module 08 Quiz

1. Which clause is used to filter query results after `GROUP BY` aggregation?
   - A) `WHERE`
   - B) `HAVING`
   - C) `FILTER`
   - D) `ORDER BY`

2. If a column contains values `[10, 20, NULL, 30]`, what will `AVG(column)` return?
   - A) 15 (sum divided by 4)
   - B) 20 (sum divided by 3, ignoring NULL)
   - C) NULL
   - D) Error
