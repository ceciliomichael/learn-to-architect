# Module 10 Exercise

1. Write a CTE named `avg_dept_salaries` that calculates average salary per department ID from `employees`.
2. Join the CTE with `departments` to output department name and average salary.
