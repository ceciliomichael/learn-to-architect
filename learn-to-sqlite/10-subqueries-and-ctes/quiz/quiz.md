# Module 10 Quiz

1. Which keyword introduces a Common Table Expression (CTE) in SQL?
   - A) `SUBQUERY`
   - B) `WITH`
   - C) `DEFINE`
   - D) `AS`

2. How does `EXISTS` evaluate its subquery?
   - A) It counts all matching rows and returns the count.
   - B) It checks whether the subquery returns at least one row, stopping immediately upon first match.
   - C) It compares values using dynamic storage classes.
   - D) It compiles a virtual FTS5 index.
