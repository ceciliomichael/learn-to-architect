# Module 07 Quiz

1. What happens if you run `DELETE FROM tasks;` without a `WHERE` clause?
   - A) SQLite asks for confirmation before deleting.
   - B) Only the first row is deleted.
   - C) All rows in the `tasks` table are permanently deleted.
   - D) An error is thrown requiring a `WHERE` clause.

2. Why is a preview `SELECT` performed prior to `UPDATE` or `DELETE`?
   - A) To verify which exact rows will be impacted by the filter condition.
   - B) To lock the database table permanently.
   - C) To index the table before writing.
   - D) To satisfy SQLite syntax requirement.
