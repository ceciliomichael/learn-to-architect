# Module 07 Quiz Answers

1. **Answer: C**
   - **Reasoning**: Omitting a `WHERE` clause in a `DELETE` statement causes SQLite to match and remove every row in the table.

2. **Answer: A**
   - **Reasoning**: Performing a preview `SELECT` with the exact same `WHERE` criteria allows you to confirm which records will be changed or destroyed before committing data mutations.
