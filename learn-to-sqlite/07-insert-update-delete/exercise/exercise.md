# Module 07 Exercise

1. Create a table `tasks` (`id` PRIMARY KEY, `description` TEXT, `completed` INT DEFAULT 0).
2. Insert 3 tasks.
3. Write a safe update sequence to mark task ID 2 completed (`1`), using preview-first `SELECT`.
4. Delete all completed tasks using preview-first `SELECT`.
