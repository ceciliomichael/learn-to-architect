# Module 06 Quiz

1. Which SQLite PRAGMA statement enables foreign key enforcement?
   - A) `PRAGMA enable_fk = TRUE;`
   - B) `PRAGMA foreign_keys = ON;`
   - C) `SET FOREIGN_KEY_CHECKS = 1;`
   - D) `ALTER TABLE ENABLE FOREIGN KEYS;`

2. What is the effect of `ON DELETE CASCADE` in a foreign key constraint?
   - A) Prevents deletion of parent rows.
   - B) Automatically deletes child rows when their matching parent row is deleted.
   - C) Sets child foreign key values to NULL.
   - D) Triggers an email notification to the administrator.
