# Module 14 Exercise

1. Using `employees` (`name`, `department`, `salary`), write a query using `ROW_NUMBER()` partitioned by `department` and ordered by `salary DESC`.
2. Filter for only the top 1 highest paid employee in each department using a CTE wrapper.
