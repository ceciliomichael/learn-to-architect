# Module 14 Quiz

1. Which window function fetches a column value from the immediately preceding row?
   - A) `LEAD()`
   - B) `LAG()`
   - C) `FIRST_VALUE()`
   - D) `ROW_NUMBER()`

2. What clause divides a window function target set into distinct groups?
   - A) `GROUP BY`
   - B) `PARTITION BY`
   - C) `WINDOW BY`
   - D) `SPLIT BY`
