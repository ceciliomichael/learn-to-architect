# Module 14 Quiz Answers

1. **Answer: B**
   - **Reasoning**: `LAG(column, offset)` accesses data from a preceding row at a given offset within the current partition.

2. **Answer: B**
   - **Reasoning**: `PARTITION BY` splits the row set into partitions for window function processing, similar to `GROUP BY` but retaining individual row outputs.
