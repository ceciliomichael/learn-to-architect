# Module 05 Exercise

1. Create a `STRICT` table named `products_strict` with:
   - `product_id` INT PRIMARY KEY
   - `title` TEXT NOT NULL
   - `price` REAL NOT NULL
2. Insert 2 valid rows into `products_strict`.
3. Try to insert a row with a text string for `price` to observe the strict validation error.
