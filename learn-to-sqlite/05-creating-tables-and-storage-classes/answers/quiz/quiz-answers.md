# Module 05 Quiz Answers

1. **Answer: C**
   - **Reasoning**: SQLite's 5 native storage classes are `NULL`, `INTEGER`, `REAL`, `TEXT`, and `BLOB`. `VARCHAR` is a type name that maps to `TEXT` affinity.

2. **Answer: A**
   - **Reasoning**: Appending `STRICT` to the end of a `CREATE TABLE` definition switches SQLite from dynamic affinity to strict type enforcement.
