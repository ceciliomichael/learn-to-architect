# Module 04 Exercise

1. Using the `scores` table, write a query to fetch the 2nd and 3rd highest scores with player names.
2. Sort primarily by `score DESC` and secondarily by `player ASC`.
3. Use `LIMIT` and `OFFSET`.
