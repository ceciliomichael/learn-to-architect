# Module 04 Quiz

1. Which statement correctly fetches rows 11 to 20 when ordered by ID?
   - A) `SELECT * FROM items ORDER BY id LIMIT 20 OFFSET 10;`
   - B) `SELECT * FROM items ORDER BY id LIMIT 10 OFFSET 10;`
   - C) `SELECT * FROM items ORDER BY id OFFSET 10 LIMIT 20;`
   - D) `SELECT DISTINCT 10, 20 FROM items ORDER BY id;`

2. What is the default sorting order if `ASC` or `DESC` is omitted in `ORDER BY`?
   - A) `DESC`
   - B) `ASC`
   - C) Random order
   - D) Insertion order
