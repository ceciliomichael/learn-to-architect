# Module 12 Quiz

1. What tool command discloses whether SQLite uses a full table scan or an index lookup?
   - A) `PRAGMA index_info;`
   - B) `EXPLAIN QUERY PLAN`
   - C) `SHOW INDEXES`
   - D) `.schema`

2. What is a trade-off of adding indexes to a table?
   - A) Faster `INSERT` and `DELETE` operations.
   - B) Slower `SELECT` queries.
   - C) Increased disk storage and slower write performance (`INSERT`/`UPDATE`/`DELETE`).
   - D) Disabling foreign key constraints automatically.
