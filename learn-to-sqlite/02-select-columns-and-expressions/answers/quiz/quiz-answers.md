# Module 02 Quiz Answers

1. **Answer: B**
   - **Reasoning**: SQLite uses `||` for string concatenation (e.g., `'a' || 'b'`). The `+` operator in SQLite is strictly numeric addition.

2. **Answer: B**
   - **Reasoning**: A `SELECT` query calculates expressions dynamically for display in the result set. It never mutates or updates underlying table rows stored on disk.
