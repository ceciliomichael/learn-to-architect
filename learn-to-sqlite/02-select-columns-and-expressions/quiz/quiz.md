# Module 02 Quiz

1. Which operator is used for string concatenation in SQLite?
   - A) `+`
   - B) `||`
   - C) `CONCAT()`
   - D) `&`

2. What does `SELECT price * 0.9 AS price FROM items;` do?
   - A) Modifies the price on disk permanently.
   - B) Calculates a 10% discount dynamically for the query output.
   - C) Throws a syntax error because column aliases cannot match existing columns.
   - D) Deletes 10% of rows from `items`.
