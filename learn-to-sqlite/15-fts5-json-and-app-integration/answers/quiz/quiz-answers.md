# Module 15 Quiz Answers

1. **Answer: B**
   - **Reasoning**: FTS5 virtual tables use the `MATCH` keyword (e.g., `WHERE table MATCH 'query'`) to execute full-text search queries.

2. **Answer: A**
   - **Reasoning**: `json_extract(json, path)` is SQLite's built-in scalar function for retrieving values from JSON formatted text columns.
