# Module 15 Quiz

1. Which operator is used to search FTS5 virtual tables in SQLite?
   - A) `LIKE`
   - B) `MATCH`
   - C) `CONTAINS`
   - D) `SEARCH`

2. What function extracts values from JSON text fields in SQLite?
   - A) `json_extract()`
   - B) `json_parse()`
   - C) `json_value()`
   - D) `get_json()`
