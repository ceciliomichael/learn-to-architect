# Module 15 Exercise

1. Create a virtual table `articles` using `fts5(heading, content)`.
2. Insert 2 articles.
3. Query `articles` using `MATCH 'SQLite'`.
4. Write a `json_extract` query against a JSON field containing `{"status": "active"}`.
