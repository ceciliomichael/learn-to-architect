# Module 01 Quiz Answers

1. **Answer: B**
   - **Reasoning**: SQLite is an embedded database engine compiled directly into an application or CLI executable. It reads and writes directly to disk files without network ports or background server daemons.

2. **Answer: C**
   - **Reasoning**: Dot-commands are specific to the `sqlite3` command-line utility. `.headers on` switches on column headers in the CLI display output.

3. **Answer: B**
   - **Reasoning**: SQLite waits for a semicolon `;` before evaluating and executing any SQL statement. Without `;`, the terminal shows `...> ` waiting for statement completion.
