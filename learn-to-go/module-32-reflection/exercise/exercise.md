# Exercises: Reflection

1. Print type and kind for integers, strings, slices, and one struct.
2. Handle a nil interface without panic.
3. List exported struct field names and JSON tags.
4. Set one integer field through a non-nil pointer after checking `CanSet`.
5. Rewrite one reflection use with a small interface or generic function.
