# Quiz: Pointers, Values, and Mutation

1. What does `&value` produce?
2. What does `*pointer` access?
3. What is a pointer's zero value?
4. Does passing a pointer avoid all copying?
5. Is a pointer required to change an existing slice element?
