# Quiz: Packages, Main, Imports, and Output

1. Which package name marks an executable command?
2. Which function is the command entry point?
3. Why is `fmt.Println` called a qualified name?
4. Does `fmt.Print` automatically end every call with a newline?
5. What happens to an unused import?
