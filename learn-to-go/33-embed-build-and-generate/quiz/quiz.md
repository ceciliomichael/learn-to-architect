# Quiz: Embedded Files, Build Constraints, and Generation

1. When are embedded files read?
2. Should secrets be embedded in a binary?
3. Does `go build` automatically run `go generate`?
4. Why test every supported build target?
5. What extra difficulty can CGO add to cross-compilation?
