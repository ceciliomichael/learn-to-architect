# Quiz Answers: Booleans, Conditions, and Switch

1. No. Go requires an expression whose type is `bool`.
2. A broad lower condition would otherwise capture higher scores before their specific branch is checked.
3. Both operands must be true for the combined result to be true.
4. No. Go stops after the matching case unless `fallthrough` is explicitly used.
5. In the condition and all branches attached to that `if`, but not after it.
