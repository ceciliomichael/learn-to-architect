# Quiz: Read Input, Parse Text, and Format Output

1. What type does terminal input initially have?
2. What two values does `strconv.Atoi` return?
3. Why call `TrimSpace` before checking an answer?
4. Where should a diagnostic normally be written?
5. Can the official Playground provide ordinary interactive input?
