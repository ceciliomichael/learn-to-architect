# Exercises: Generics

1. Write generic `Last` with an empty-slice boolean.
2. Write `Contains` using `comparable`.
3. Create a generic `Pair` type.
4. Add a method that returns the pair values in reverse order as a new pair.
5. Compare one appropriate and one unnecessary use of generics.
