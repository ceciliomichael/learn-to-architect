# Quiz Answers: Strings, Bytes, Runes, and Unicode

1. The number of bytes in the string.
2. One Unicode code point, represented with the `int32` type alias.
3. No. They create one rune literal.
4. One visible grapheme can contain more than one Unicode code point.
5. No. A byte boundary may cut through a multi-byte UTF-8 encoding.
