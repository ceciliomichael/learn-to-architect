# Quiz: SQL from Go

1. Is `*sql.DB` one database connection?
2. Can a placeholder bind a table name?
3. What error means a one-row query matched nothing?
4. What must be checked after a rows loop?
5. Why defer transaction rollback before doing transaction work?
