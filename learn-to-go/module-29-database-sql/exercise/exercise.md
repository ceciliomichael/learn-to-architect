# Exercises: SQL from Go

1. Create a disposable SQLite database with a reviewed pure-Go driver.
2. Create a constrained products table.
3. Insert three rows in one transaction using parameters.
4. Query one external name with a bound parameter and handle no rows.
5. Stream ordered rows, close them, and check `rows.Err`.
