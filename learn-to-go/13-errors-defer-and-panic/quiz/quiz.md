# Quiz: Errors, Defer, Panic, and Recovery Boundaries

1. What does a nil error mean by convention?
2. Which formatting verb wraps an error cause?
3. Why use `errors.Is` instead of comparing text?
4. When do deferred calls run?
5. Is recover a replacement for checking returned errors?
