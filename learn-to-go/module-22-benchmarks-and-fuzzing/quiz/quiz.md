# Quiz: Benchmarks and Fuzz Tests

1. What does `b.N` control?
2. Why keep setup outside measured work?
3. What is a fuzz seed?
4. What happens to a failing generated input?
5. Can one benchmark prove every workload is faster?
