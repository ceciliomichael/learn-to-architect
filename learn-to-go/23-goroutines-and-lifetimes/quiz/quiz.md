# Quiz: Goroutines and Lifetimes

1. What does the `go` keyword do?
2. Is goroutine execution order guaranteed?
3. When should `WaitGroup.Add` run?
4. What must each counted worker call?
5. Does a wait group prevent data races?
