# Quiz: Numbers, Operators, and Conversions

1. What is `7 / 2` when both values are integers?
2. What does `%` calculate?
3. Why is `price * quantity` rejected when their types are `float64` and `int`?
4. Does `%.2f` change the stored floating-point value?
5. Why can unsigned integers still require range validation?
