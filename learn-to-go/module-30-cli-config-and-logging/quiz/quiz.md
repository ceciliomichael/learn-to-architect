# Quiz: Command-Line Programs, Configuration, and Logging

1. Why use a dedicated FlagSet?
2. Are environment variables trusted typed values?
3. Where should normal command output go?
4. Why call `os.Exit` only at the outer main boundary?
5. Should a token be logged at debug level?
