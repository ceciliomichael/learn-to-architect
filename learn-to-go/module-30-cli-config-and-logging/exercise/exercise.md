# Exercises: Command-Line Programs, Configuration, and Logging

1. Parse a required input path, positive limit, and verbose flag.
2. Validate an allowed mode from an environment variable.
3. Return 2 for invalid input and 1 for a file operation failure.
4. Send results to stdout and diagnostics to stderr.
5. Log a safe event with `slog` while omitting file contents and secrets.
