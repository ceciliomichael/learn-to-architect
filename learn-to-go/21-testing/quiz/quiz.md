# Quiz: Tests with the Testing Package

1. What suffix identifies a Go test file?
2. What does `t.Run` create?
3. Why call `t.Helper` in a helper?
4. What does `t.TempDir` provide?
5. Does 100 percent statement coverage prove correctness?
