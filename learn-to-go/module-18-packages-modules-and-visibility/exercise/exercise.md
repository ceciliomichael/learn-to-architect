# Exercises: Packages, Modules, and Visibility

1. Create a module with a `price` library package and one command.
2. Export subtotal while keeping one validation helper private.
3. Add package documentation and run `go doc`.
4. Run `go test ./...`, `go vet ./...`, and `go mod tidy`.
5. Draw package dependencies and prove no arrow forms a cycle.
