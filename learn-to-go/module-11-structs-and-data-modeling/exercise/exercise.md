# Exercises: Structs and Data Modeling

1. Define a `Book` with ID, title, price in cents, and availability.
2. Create two keyed values and print selected fields.
3. Observe zero values from `var empty Book`.
4. Copy a struct with a slice field and demonstrate shared elements.
5. Repair the sharing with an explicit slice copy.
