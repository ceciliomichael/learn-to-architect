# Quiz: Strings, Bytes, Runes, and Unicode

1. What does `len` report for a string?
2. What does a rune represent?
3. Do single quotes create a string?
4. Why can rune count differ from visible character count?
5. Is arbitrary byte slicing safe for user-visible Unicode text?
