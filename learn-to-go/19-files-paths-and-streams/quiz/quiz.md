# Quiz: Files, Paths, and Streams

1. When should cleanup be deferred?
2. Why can `os.ReadFile` be unsafe for unbounded input?
3. Which package handles operating-system paths?
4. Is checking a filename extension sufficient authorization?
5. Why can a write-side close error matter?
