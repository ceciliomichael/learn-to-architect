# Quiz: Structs and Data Modeling

1. What do omitted struct fields contain?
2. Why prefer keyed literals in ordinary application code?
3. Does struct assignment always create deep independence?
4. When can two struct values be compared with `==`?
5. Do struct tags perform runtime validation by themselves?
