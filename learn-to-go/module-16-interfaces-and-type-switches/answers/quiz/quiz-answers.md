# Quiz Answers: Interfaces and Type Switches

1. No. Satisfaction follows automatically from its method set.
2. Near the code that consumes the required behavior.
3. The interface still stores a dynamic pointer type even though that pointer's value is nil.
4. It reports mismatch with `ok == false` instead of panicking.
5. `Error() string`.
