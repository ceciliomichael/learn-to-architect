# Quiz: HTTP Clients

1. Why reuse an HTTP client?
2. Does status 500 make `client.Do` return an error automatically?
3. Why close the body?
4. Why limit the body before decoding?
5. Why can user-controlled URLs be dangerous from a server?
