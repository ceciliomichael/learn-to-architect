# Exercises: Benchmarks and Fuzz Tests

1. Benchmark list membership and map membership with representative sizes.
2. Report allocations and record the Go version and machine context.
3. Fuzz a UTF-8-safe reverse function using a double-reverse property.
4. Add empty, ASCII, and multi-byte seeds.
5. Run seed tests normally, then a ten-second bounded fuzz session.
