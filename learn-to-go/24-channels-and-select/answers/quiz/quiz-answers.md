# Quiz Answers: Channels and Select

1. It waits until a receiver participates.
2. The owner responsible for all sending, after every send is finished.
3. No. Close only announces that no more values will be sent.
4. It returns `ok == false` with the element type's zero value.
5. The loop can repeatedly choose default without blocking or making useful progress.
