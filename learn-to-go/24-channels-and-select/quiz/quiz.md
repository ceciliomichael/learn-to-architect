# Quiz: Channels and Select

1. What happens on an unbuffered send before a receiver is ready?
2. Who normally closes a channel?
3. Does closing release a channel like closing a file?
4. What does comma-ok report after values are drained?
5. Why can `select` with `default` waste CPU?
