# Exercises: Tests with the Testing Package

1. Test subtotal with ordinary, zero, and invalid values.
2. Use a table and named subtests.
3. Test an expected sentinel error with `errors.Is`.
4. Test UTF-8 file writing inside `t.TempDir`.
5. Run `go test -cover ./...` and explain what coverage cannot prove.
