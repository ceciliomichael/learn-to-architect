# Quiz: Generics

1. What does a type parameter represent for one call?
2. Does `any` permit every operator?
3. Why does `Contains` need `comparable`?
4. Are generics the same as interface-based runtime dispatch?
5. When is concrete code preferable?
