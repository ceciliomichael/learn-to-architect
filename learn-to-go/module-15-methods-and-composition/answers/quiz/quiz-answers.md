# Quiz Answers: Methods, Embedding, and Composition

1. A receiver between `func` and the method name.
2. When the method must mutate the receiver or pointer method-set semantics are intended.
3. No. It composes a value and can promote names.
4. Every supported state change then has one place to enforce the invariant.
5. When the relationship needs a domain name or promoted names would hide ownership.
