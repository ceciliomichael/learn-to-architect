# Quiz: Methods, Embedding, and Composition

1. What extra declaration distinguishes a method from a function?
2. When is a pointer receiver normally required?
3. Does embedding create class inheritance?
4. Why keep a balance rule inside methods?
5. When can a named field be clearer than embedding?
