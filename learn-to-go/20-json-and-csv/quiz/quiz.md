# Quiz: JSON and CSV Boundaries

1. Do JSON struct tags validate domain rules?
2. What does `DisallowUnknownFields` change?
3. Why bound data before decoding?
4. What type are CSV fields initially?
5. Does successful deserialization make data trusted?
