# Quiz Answers: JSON and CSV Boundaries

1. No. They map field names and options for the codec.
2. Object fields with no matching exported struct field cause decoding to fail.
3. To prevent excessive memory, CPU, or storage use from untrusted input.
4. Strings that require deliberate conversion and validation.
5. No. Meaning, authorization, ranges, and cross-field rules still require validation.
