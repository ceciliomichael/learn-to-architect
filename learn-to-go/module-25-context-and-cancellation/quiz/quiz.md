# Quiz: Context and Cancellation

1. Where does a context parameter normally appear?
2. Why call the cancel function even when a deadline exists?
3. Does cancellation forcibly stop every goroutine?
4. Should ordinary optional settings be stored in context?
5. Why derive from the incoming context?
