# Quiz: Variables, Constants, and Basic Types

1. When can `:=` be used?
2. What is the zero value of `bool`?
3. What is the zero value of `string`?
4. Can an `int` variable later store a string?
5. Should a deployment setting normally be hardcoded as a constant?
