# Quiz: Booleans, Conditions, and Switch

1. Can an integer be used directly as an `if` condition?
2. Why do score branches begin with the highest boundary?
3. What does `&&` require?
4. Do switch cases fall through automatically?
5. Where does a name declared in an `if` initializer exist?
