# Quiz Answers: Use the Go Playground and Install Go

1. It checks and translates Go source into executable machine code.
2. **Format** applies Go's standard source layout.
3. `go run` builds a temporary program and immediately runs it. `go build` leaves an executable.
4. No. It runs in a sandbox with file, network, time, CPU, and memory restrictions.
5. It usually identifies the earliest cause. Later messages may only be consequences of that first mistake.
