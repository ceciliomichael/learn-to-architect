# Quiz: Arrays and Slices

1. Is an array's length part of its type?
2. What storage does a slice describe?
3. Why must the result of `append` be assigned?
4. Does assigning one slice to another copy its elements?
5. Can a nil slice be appended to?
