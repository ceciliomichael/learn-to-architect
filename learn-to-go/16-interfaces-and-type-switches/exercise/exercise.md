# Exercises: Interfaces and Type Switches

1. Define a two-method reader interface at a consumer.
2. Satisfy it with an in-memory type without an explicit declaration.
3. Define a custom stock error with structured counts.
4. Use `errors.As` to inspect that error after wrapping.
5. Write a type switch for a deliberately small set of display values.
