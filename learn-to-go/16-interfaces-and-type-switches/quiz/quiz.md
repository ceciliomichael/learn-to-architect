# Quiz: Interfaces and Type Switches

1. Does a type declare that it implements an interface?
2. Where should a small interface normally be defined?
3. Why can an interface containing a typed nil pointer be non-nil?
4. What does the comma-ok form of a type assertion prevent?
5. Which method makes a type satisfy `error`?
