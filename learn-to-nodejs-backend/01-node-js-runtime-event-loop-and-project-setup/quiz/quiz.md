# Quiz: Node.js Runtime, Event Loop, and TypeScript Project Setup

1. What responsibilities belong to TypeScript, `@types/node`, and `tsx`?
2. Why does the course run `tsc --noEmit` even when `tsx` can execute the source?
3. What is the purpose of `rootDir` and `outDir`?
4. In the lesson example, why does the promise callback run before the zero-delay timer?
5. Which project files should be committed and which two directories should be regenerated?
