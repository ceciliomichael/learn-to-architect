# Module 23 Quiz Answers

## Answer 1

It is `"x" | "y"`.

## Answer 2

It is `number`.

## Answer 3

The key must belong to the object, and the return type matches the value type of that exact key.

## Answer 4

Runtime `typeof` returns a category string such as `"object"`. Type-position `typeof` captures the static type of the named value.

## Answer 5

The string might name no property on the object. The key needs proof such as `keyof` or a runtime check.
