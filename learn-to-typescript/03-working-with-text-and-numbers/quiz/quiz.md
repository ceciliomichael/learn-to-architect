# Module 03 Quiz

## Question 1

What is the output?

```typescript
const name = "Ari";
const points = 12;
console.log(`${name} has ${points + 3} points.`);
```

## Question 2

Which expression is a string property, and which are string method calls?

```typescript
word.length
word.toUpperCase()
word.toLowerCase()
```

## Question 3

Predict both results:

```typescript
console.log(2 + 3 * 4);
console.log((2 + 3) * 4);
```

## Question 4

What type does `(10 / 3).toFixed(2)` return, and why can that be useful?

## Question 5

Correct the code so it calculates `150` as a number:

```typescript
const price = "50";
const quantity = 3;
const total = price + quantity;
```

Then read the [quiz answers](../answers/quiz/quiz-answers.md).
