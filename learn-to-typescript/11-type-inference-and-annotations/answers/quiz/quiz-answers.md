# Module 11 Quiz Answers

## Answer 1

Type inference is TypeScript working out a static type from values and surrounding code without a written annotation.

## Answer 2

**B** usually adds more information. An empty array does not show its intended item type. In **A**, the string value already makes the basic type obvious.

## Answer 3

The array method provides context. TypeScript knows the array items are numbers, so it knows the callback receives a number.

## Answer 4

No. It is a type error. An annotation checks an expectation but does not perform runtime conversion.

## Answer 5

They are removed. The emitted JavaScript contains the executable code, not ordinary type annotations.
