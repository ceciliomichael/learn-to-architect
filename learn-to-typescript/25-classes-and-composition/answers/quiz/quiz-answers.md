# Module 25 Quiz Answers

## Answer 1

The `new` operator creates an instance, and the class constructor initializes it.

## Answer 2

No. It checks that the class declaration satisfies the interface. The class must implement its own behavior.

## Answer 3

TypeScript private is mainly a static access restriction and is usually erased. A JavaScript `#private` field has runtime language enforcement.

## Answer 4

Composition is often clearer for has-a or uses-a relationships, replaceable dependencies, and cases where inheritance would create an artificial hierarchy.

## Answer 5

Add one when callers need structured error data or must distinguish that error at runtime. A built-in Error or result union is enough for simpler cases.
