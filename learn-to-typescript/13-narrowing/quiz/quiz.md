# Module 13 Quiz

## Question 1

Why can `value: string | number` not immediately use `value.toUpperCase()`?

## Question 2

What type does TypeScript understand inside this block?

```typescript
if (typeof value === "number") {
  // value is what type here?
}
```

## Question 3

What does `typeof null` return in JavaScript?

## Question 4

Why can `if (value)` be wrong for `value: number | undefined` when zero is valid?

## Question 5

What check distinguishes `string` from `string[]` reliably?

Then read the [quiz answers](../answers/quiz/quiz-answers.md).
