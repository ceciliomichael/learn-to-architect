# Module 07 Quiz

## Question 1

For the array `const names = ["Ana", "Bo", "Cy"];`, what are the first index, final index, and length?

## Question 2

What can `pop()` return when an array is empty?

## Question 3

Does `const values = [1, 2];` prevent `values.push(3)`? Explain.

## Question 4

What is the output?

```typescript
const numbers = [2, 3, 4];
let total = 0;

for (const number of numbers) {
  total = total + number;
}

console.log(total);
```

## Question 5

Why can an arbitrary array index be unsafe even when the array item type is `string`?

Then read the [quiz answers](../answers/quiz/quiz-answers.md).
