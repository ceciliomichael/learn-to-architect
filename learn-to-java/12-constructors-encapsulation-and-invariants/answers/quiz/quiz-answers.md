# Quiz answers

1. A constructor initializes a new object and has no return type.
2. Private fields prevent unrelated code from changing state directly.
3. An invariant is a rule every observable object state must satisfy.
4. Reject invalid constructor and method inputs before changing fields.
5. An accessor exposes only the information callers need.

