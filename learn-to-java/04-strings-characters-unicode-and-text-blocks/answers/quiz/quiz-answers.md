# Quiz answers

1. Strings are immutable, so operations return new values rather than changing existing text.
2. Use equals for text content because == compares object references.
3. A char is one UTF-16 code unit and may be only part of a Unicode code point.
4. Use codePoints when complete Unicode code points matter.
5. Text blocks make intentional multi-line source text readable.

