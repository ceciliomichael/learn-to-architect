# Quiz answers

1. @Test marks behavior the JUnit engine should execute.
2. Assertions compare observable results with the contract.
3. assertThrows verifies an expected exception without hiding unrelated failures.
4. Tests must not depend on execution order or shared mutable state.
5. JUnit belongs in the test dependency scope, not the production runtime.

