# Exercise: JVM Memory, Garbage Collection, and Resources

1. Recreate the example or project boundary in a disposable folder.
2. Verify it with javac --release 21 -Xlint:all Main.java, then java Main.
3. Confirm this behavior: true followed by a warning about collection timing.
4. Find three resources that require close even though their wrapper objects are garbage collected.
5. Explain how every listed contract is enforced and which evidence proves it.

Keep credentials, personal data, generated caches, and publish actions outside the exercise.

