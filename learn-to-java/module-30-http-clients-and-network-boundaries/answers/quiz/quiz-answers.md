# Quiz answers

1. Reuse one HttpClient so configuration and connections are shared.
2. Parse the URI and restrict schemes, hosts, and redirects when input controls the destination.
3. Set connect and request timeouts based on an application deadline.
4. Treat non-success status codes according to the remote API contract.
5. Limit response bytes before decoding and validate all decoded values.

