# Quiz answers

1. An interface names behavior without deciding one storage design.
2. A class implements every required abstract method.
3. Composition models a has-a or uses-a relationship.
4. Passing a collaborator makes dependencies visible and replaceable in tests.
5. A default interface method should provide behavior valid for all implementations.

