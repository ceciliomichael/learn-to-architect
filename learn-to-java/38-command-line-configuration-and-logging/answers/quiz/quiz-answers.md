# Quiz answers

1. Document whether command line, environment, file, or default wins for each setting.
2. Domain code should receive validated typed configuration rather than raw strings.
3. Use zero for success and a documented nonzero status for failure.
4. Normal output belongs on stdout and diagnostics belong on stderr.
5. Use stable fields and levels while excluding secrets and sensitive payloads.

