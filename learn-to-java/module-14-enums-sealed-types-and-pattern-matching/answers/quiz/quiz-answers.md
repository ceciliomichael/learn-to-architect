# Quiz answers

1. An enum defines named instances from a closed set.
2. A sealed class or interface limits which types may directly implement or extend it.
3. A type pattern tests a value and introduces a safely typed local name.
4. A switch over a sealed hierarchy can omit default when every permitted type is covered.
5. Adding a permitted subtype or enum constant requires reviewing exhaustive consumers.

