# Quiz answers

1. Jackson 3 uses tools.jackson package names and must be installed through the reviewed build.
2. Deserialization checks representable shape, not business validity or authorization.
3. Choose deliberately whether unknown fields are rejected for a strict contract.
4. Limit bytes and nesting before parsing untrusted JSON.
5. Convert a raw input record into a validated domain type before behavior uses it.

