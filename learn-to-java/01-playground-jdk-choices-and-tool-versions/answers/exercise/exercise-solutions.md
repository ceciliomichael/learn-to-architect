# Exercise solution

```java
public class Main {
    public static void main(String[] args) {
        System.out.println("Java 21 course baseline");
    }
}
```

Choose a Java environment deliberately instead of treating every Java installation as identical. The program demonstrates each rule listed in the lesson and produces Java 21 course baseline.

For the practice extension, change one behavior at a time and recompile immediately. If your implementation differs, it is still correct when it preserves validation, ownership, and observable behavior.

