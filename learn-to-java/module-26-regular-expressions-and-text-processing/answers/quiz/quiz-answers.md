# Quiz answers

1. Compile a reused regular expression once as a Pattern.
2. Matcher holds state for one input and should not be shared carelessly.
3. matches requires the entire input; find searches for a matching region.
4. Pattern.quote treats external text literally instead of as regex syntax.
5. Avoid vulnerable nested repetition and set input limits for untrusted text.

