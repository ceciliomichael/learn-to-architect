# Quiz answers

1. A stream pipeline is lazy until a terminal operation requests values.
2. map and filter describe transformations without changing the source collection.
3. collect, count, reduce, and forEach consume a stream pipeline.
4. Optional represents a result that may be absent and is mainly useful as a return type.
5. Use a loop when a pipeline hides branching, checked failures, or important state.

