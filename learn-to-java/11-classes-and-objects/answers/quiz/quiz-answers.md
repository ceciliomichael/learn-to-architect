# Quiz answers

1. A class declares the fields and methods available to its instances.
2. new creates an object and returns a reference to it.
3. Each object has its own instance-field values unless a field is static.
4. An instance method acts on the object referenced by this.
5. Assigning an object reference does not clone the object.

