# Quiz answers

1. A type parameter stands for a caller-selected reference type.
2. List<Integer> is not a subtype of List<Number>.
3. ? extends T is useful for reading values as T.
4. ? super T is useful for safely adding T values.
5. Most generic type arguments are erased at runtime, so some type tests and arrays are restricted.

