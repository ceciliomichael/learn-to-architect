# Quiz answers

1. A subtype must honor expectations established by its parent contract.
2. @Override asks the compiler to verify that a method actually replaces an inherited method.
3. A call through a parent reference selects the runtime object's override.
4. final can prevent unsafe extension or overriding.
5. Prefer composition when behavior varies independently or the relationship is uses-a.

