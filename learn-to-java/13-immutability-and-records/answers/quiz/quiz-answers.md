# Quiz answers

1. An immutable type does not expose a state change after construction.
2. Mutable arrays and collections need copying at immutable boundaries.
3. A record supplies final components, accessors, equality, hashing, and a readable toString.
4. A record compact constructor can validate and normalize components.
5. A record may contain methods, but its components still describe its state.

