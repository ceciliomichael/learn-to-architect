# Quiz answers

1. An array length is fixed when the array is created.
2. Valid indexes run from zero through length minus one.
3. Assigning an array variable shares the same array rather than copying elements.
4. Use Arrays.copyOf when independent storage is required.
5. main receives untrusted command-line strings that require count and value checks.

