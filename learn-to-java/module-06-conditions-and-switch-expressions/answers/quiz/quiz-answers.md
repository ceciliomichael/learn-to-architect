# Quiz answers

1. An if condition must be boolean; Java does not treat numbers as truth values.
2. The first matching branch runs, so broader conditions must not hide narrower cases.
3. && and || may skip the right operand when the result is already known.
4. A switch expression yields a value and uses arrow cases without accidental fall-through.
5. Test immediately below, at, and above each threshold.

