# Exercises: Values, Variables, and Types

1. Create names for a course title, module number, price, availability, and optional note.
2. Print every value and type.
3. Increase the module number by rebinding its name.
4. Test whether the optional note is `None`.
5. Intentionally use a name before assignment, read the error, then fix the order.
