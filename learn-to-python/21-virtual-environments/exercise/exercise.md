# Exercises: Virtual Environments and Dependencies

1. Create `.venv` in a disposable project and activate it.
2. Print `sys.executable` and pip's path.
3. List installed packages without installing a new dependency.
4. Record the environment, create a second fresh environment, and reproduce it.
5. Add `.venv/` to the project's `.gitignore` and explain why.
