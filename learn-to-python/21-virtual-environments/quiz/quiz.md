# Quiz: Virtual Environments and Dependencies

1. What does a virtual environment isolate?
2. Why use `python -m pip`?
3. Should `.venv` be committed?
4. What limitation does `pip freeze` have?
5. Why is installing a dependency a trust decision?
