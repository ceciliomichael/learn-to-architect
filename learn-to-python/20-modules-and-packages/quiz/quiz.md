# Quiz: Modules, Imports, and Packages

1. What happens on a module's first import?
2. What value does `__name__` have in the entry script?
3. Why use a main guard?
4. Why avoid wildcard imports?
5. What is a common cure for circular imports?
