# Quiz: Define and Call Functions

1. What does `def` create?
2. How does a parameter differ from an argument?
3. What happens when execution reaches return?
4. What is returned implicitly?
5. Why prefer returning over printing for calculations?
