# Exercises: Context Managers

1. Create Announce and prove exit runs after an exception.
2. Create timer context with `contextmanager` and `time.perf_counter`.
3. Manage two temporary files with ExitStack.
4. Write a manager that suppresses only one custom expected exception and prove other exceptions propagate.
5. List three non-file resources suited to context management.
