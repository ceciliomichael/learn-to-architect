# Quiz Answers: Dataclasses and Enums

1. Initializer, representation, and equality by default.
2. It creates a fresh mutable value for every instance.
3. No.
4. Ordinary per-instance attribute storage and adding undeclared attributes.
5. Call the enum class with that value and handle `ValueError`.
