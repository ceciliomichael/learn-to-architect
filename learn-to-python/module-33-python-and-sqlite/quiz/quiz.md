# Quiz: Use SQLite Safely from Python

1. Does the connection context close the connection?
2. What does it do on a block exception?
3. Why must a one-parameter sequence include a comma?
4. Can a parameter bind a column name?
5. Why avoid fetchall for an unbounded result?
