# Exercises: Project Structure, Packaging, and Secure Boundaries

1. Create the lesson source layout with working normalize_title and CLI main.
2. Add the complete pyproject metadata and install it editable in a fresh environment.
3. Add a packaged UTF-8 resource and load it with importlib.resources.
4. Write validation for CLI text length and allowed mode before domain work.
5. Run a fixed subprocess argument list with timeout and explain why shell text is rejected.
6. Build and install a wheel in another environment, then run tests against it.
