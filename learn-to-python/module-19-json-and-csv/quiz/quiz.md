# Quiz: Work with JSON and CSV Data

1. Do JSON object keys support arbitrary Python objects?
2. What does a JSON tuple become after a round trip?
3. Why use `newline=""` for CSV files?
4. What type are ordinary CSV fields when read?
5. What must happen after external data parses?
