# Quiz Answers: Work with JSON and CSV Data

1. No. JSON object keys are strings.
2. A list.
3. So the CSV module can manage record newlines correctly across platforms.
4. Strings.
5. Validate structure, types, ranges, sizes, and allowed values for the domain.
