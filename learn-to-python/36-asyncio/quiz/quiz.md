# Quiz: Async I/O with asyncio

1. What does calling an async function return?
2. What happens at await?
3. What does TaskGroup guarantee at context exit?
4. How should cancellation normally be handled after cleanup?
5. Does to_thread create CPU process parallelism?
