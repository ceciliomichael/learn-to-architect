# Exercises: Iterators and Generators

1. Manually consume a list iterator and catch its final StopIteration.
2. Write inclusive integer range generator with positive steps and validation.
3. Write generator that yields nonempty stripped lines from an iterable.
4. Combine two generators with `yield from`.
5. Consume only the first three values of a large generator and explain what was not calculated.
