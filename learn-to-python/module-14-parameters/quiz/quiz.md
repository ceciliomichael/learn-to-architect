# Quiz: Use Parameters Clearly

1. When are default expressions evaluated?
2. Why is `[]` usually a bad default?
3. What does a bare `*` do in a signature?
4. What type receives `*args`?
5. What type receives `**kwargs`?
