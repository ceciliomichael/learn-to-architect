# Exercises: Use Parameters Clearly

1. Write a function with one required parameter and two keyword-only options.
2. Write `collect(value, values=None)` without a shared default list and prove separate calls are independent.
3. Sum any number of numeric positional arguments.
4. Build a dictionary from one required name and additional keyword fields.
5. Unpack a tuple and dictionary into calls.
