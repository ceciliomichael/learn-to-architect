# Quiz: Input and Conversion

1. What type does `input` return?
2. What does `int("12")` return?
3. What exception does `int("twelve")` raise?
4. Does `int(3.9)` round to 4?
5. Why must input not be passed to `eval`?
