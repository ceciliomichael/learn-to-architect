# Quiz: Handle Exceptions Deliberately

1. When does try `else` run?
2. When does `finally` run?
3. Why keep try blocks small?
4. What does `raise new_error from error` preserve?
5. Why is `except Exception: pass` harmful?
