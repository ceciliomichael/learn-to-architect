# Quiz: Dictionaries

1. What does dictionary membership test by default?
2. What happens when bracket lookup misses?
3. When is `get` appropriate?
4. Must dictionary keys be hashable?
5. Does `dict.copy` copy nested lists?
