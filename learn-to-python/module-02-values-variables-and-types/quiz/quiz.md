# Quiz: Values, Variables, and Types

1. What does assignment do?
2. Does a Python name have one permanent type?
3. Are `total` and `Total` the same name?
4. What does `None` represent?
5. Why avoid naming a variable `list`?
