# Quiz: Add Type Hints

1. Does Python normally enforce function annotations?
2. What does `str | None` mean?
3. Why accept `Iterable` instead of list sometimes?
4. What danger does `Any` introduce?
5. Where must external data be validated?
