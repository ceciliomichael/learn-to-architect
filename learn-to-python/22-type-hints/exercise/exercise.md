# Exercises: Add Type Hints

1. Annotate functions for rectangle area, optional lookup, and string normalization.
2. Accept an iterable of integers and return a list of positive integers.
3. Define a callable alias for an integer validator.
4. Narrow `str | None` before using `upper`.
5. Pass a wrong type at runtime and explain why annotations did not stop the call.
