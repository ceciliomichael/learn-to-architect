# Module 09 Exercise

1. Write a `LEFT JOIN` between `customers` and `orders` to list all customers regardless of whether they have placed an order.
2. Filter for customers where `orders.id IS NULL` to identify inactive customers.
