# Module 06 Quiz

1. Which built-in MySQL function returns the ID generated for an `AUTO_INCREMENT` column by the most recent `INSERT` statement?
   - A) `GET_LAST_ID()`
   - B) `LAST_INSERT_ID()`
   - C) `AUTO_ID()`
   - D) `SCOPE_IDENTITY()`

2. Which storage engine in MySQL supports foreign key constraints and transactional row locking?
   - A) MyISAM
   - B) MEMORY
   - C) InnoDB
   - D) CSV
