# Module 15 Exercise

1. Create a user `'read_only_user'@'localhost'` with password `'Secret123!'`.
2. Grant `SELECT` privileges on `store_db.*`.
3. Verify grants with `SHOW GRANTS FOR 'read_only_user'@'localhost';`.
4. Write the terminal command to perform a database backup using `mysqldump`.
