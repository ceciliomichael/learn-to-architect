# Module 04 Quiz Answers

1. **Answer: B**
   - **Reasoning**: In MySQL's comma syntax `LIMIT offset, count`, the first argument (`30`) is the offset (rows to skip), and the second argument (`10`) is the count of rows to return.

2. **Answer: B**
   - **Reasoning**: Relational tables are unordered sets. Without `ORDER BY`, row sequence can change across queries, resulting in inconsistent pagination.
