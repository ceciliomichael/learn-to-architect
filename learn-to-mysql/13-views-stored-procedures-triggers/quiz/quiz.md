# Module 13 Quiz

1. Why is `DELIMITER //` specified before creating stored procedures in the MySQL CLI?
   - A) To increase execution speed.
   - B) To replace the default semicolon `;` delimiter so statement bodies containing semicolons can be parsed as a single block.
   - C) To enable strict table mode.
   - D) To grant admin privileges.

2. How do you execute a stored procedure in MySQL?
   - A) `EXECUTE procedure_name()`
   - B) `CALL procedure_name()`
   - C) `RUN procedure_name()`
   - D) `START procedure_name()`
