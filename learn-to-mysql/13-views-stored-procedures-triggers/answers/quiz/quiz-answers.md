# Module 13 Quiz Answers

1. **Answer: B**
   - **Reasoning**: In CLI client tools, `DELIMITER //` changes the statement termination character so the server doesn't execute statements early when encountering internal semicolons within procedure or trigger bodies.

2. **Answer: B**
   - **Reasoning**: `CALL procedure_name(arguments)` is the standard SQL statement for executing stored procedures in MySQL.
