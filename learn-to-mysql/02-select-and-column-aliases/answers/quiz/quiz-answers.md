# Module 02 Quiz Answers

1. **Answer: B**
   - **Reasoning**: MySQL uses `CONCAT(str1, str2, ...)` for string concatenation. The `+` operator attempts numeric addition.

2. **Answer: A**
   - **Reasoning**: Backticks (`` `identifier` ``) are MySQL's default quoting mechanism for identifiers (table names, column names).
