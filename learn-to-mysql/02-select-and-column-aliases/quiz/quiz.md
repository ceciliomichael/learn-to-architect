# Module 02 Quiz

1. Which function is used in MySQL to concatenate multiple strings together?
   - A) `||`
   - B) `CONCAT()`
   - C) `STRING_ADD()`
   - D) `JOIN()`

2. What characters enclose SQL identifiers like column names in MySQL?
   - A) Backticks (`` `col` ``)
   - B) Single quotes (`'col'`)
   - C) Curly braces (`{col}`)
   - D) Slashes (`/col/`)
