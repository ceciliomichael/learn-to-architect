# Module 02 Exercise

1. Query `items` table in MySQL.
2. Use `CONCAT()` to generate a column `price_tag` formatted as `'ITEM: ' || title || ' Costs $' || unit_price` (adapted to MySQL `CONCAT()`).
3. Calculate a 15% discount price aliased as `discount_price`.
