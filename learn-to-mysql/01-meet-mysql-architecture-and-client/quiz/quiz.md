# Module 01 Quiz

1. What is `mysqld` in a MySQL environment?
   - A) A client command-line interface.
   - B) The background server daemon that handles database storage and processing.
   - C) A graphical workbench tool.
   - D) A backup utility script.

2. Which SQL statement sets the active default database for a session?
   - A) `OPEN database_name;`
   - B) `SELECT database_name;`
   - C) `USE database_name;`
   - D) `CONNECT database_name;`
