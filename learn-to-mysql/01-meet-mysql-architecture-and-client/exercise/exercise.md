# Module 01 Exercise

1. Open your terminal and connect to MySQL CLI (`mysql -u root -p`).
2. Create a database named `store_db`.
3. Select `store_db` using `USE store_db;`.
4. Run a query that returns your current connected user (`USER()`) and current database (`DATABASE()`).
