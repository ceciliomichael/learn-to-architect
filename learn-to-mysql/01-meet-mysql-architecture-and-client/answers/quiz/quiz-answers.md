# Module 01 Quiz Answers

1. **Answer: B**
   - **Reasoning**: `mysqld` (MySQL Server Daemon) is the core service executable that manages memory buffers, storage engines, locking, and network requests.

2. **Answer: C**
   - **Reasoning**: The `USE database_name;` command selects the target database for subsequent SQL operations in that session.
