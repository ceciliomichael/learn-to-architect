# Module 11 Quiz

1. What is the default transaction isolation level in MySQL InnoDB?
   - A) `READ UNCOMMITTED`
   - B) `READ COMMITTED`
   - C) `REPEATABLE READ`
   - D) `SERIALIZABLE`

2. How does row-level locking in InnoDB compare to table-level locking in MyISAM?
   - A) MyISAM allows higher write concurrency than InnoDB.
   - B) InnoDB locks only target rows, allowing other transactions to modify unrelated rows in the same table simultaneously.
   - C) Table locking prevents deadlocks permanently.
   - D) InnoDB disables foreign keys during locking.
