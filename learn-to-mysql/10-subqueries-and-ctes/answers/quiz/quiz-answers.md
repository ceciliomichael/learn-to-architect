# Module 10 Quiz Answers

1. **Answer: C**
   - **Reasoning**: MySQL 8.0 introduced full support for Common Table Expressions (`WITH` clause) and window functions.

2. **Answer: A**
   - **Reasoning**: CTEs are defined using the standard `WITH cte_name AS (...)` syntax preceding the main query.
