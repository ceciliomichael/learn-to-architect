# Module 08 Exercise

1. Group sales by `region` and compute total revenue (`SUM(amount)`).
2. Ensure query complies with `ONLY_FULL_GROUP_BY`.
3. Filter for regions with total revenue exceeding `500.00`.
