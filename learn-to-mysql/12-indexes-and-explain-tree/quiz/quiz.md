# Module 12 Quiz

1. Which command in MySQL 8.0+ outputs a structured tree visualization of the query plan?
   - A) `EXPLAIN FORMAT=TREE`
   - B) `EXPLAIN FORMAT=JSON`
   - C) `SHOW PLAN TREE`
   - D) `ANALYZE TABLE`

2. What rule governs how composite indexes `(colA, colB)` are evaluated by MySQL?
   - A) Rightmost Prefix Rule
   - B) Leftmost Prefix Rule
   - C) Dynamic Affinity Rule
   - D) Random Key Rule
