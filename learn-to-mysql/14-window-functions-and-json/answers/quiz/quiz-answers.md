# Module 14 Quiz Answers

1. **Answer: B**
   - **Reasoning**: The `->>` operator in MySQL extracts a JSON value and unquotes it, equivalent to `JSON_UNQUOTE(JSON_EXTRACT(col, path))`.

2. **Answer: C**
   - **Reasoning**: MySQL 8.0 added support for standard SQL window functions (`OVER()`, `ROW_NUMBER()`, `RANK()`, etc.).
