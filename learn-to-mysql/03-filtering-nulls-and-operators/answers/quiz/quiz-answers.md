# Module 03 Quiz Answers

1. **Answer: B**
   - **Reasoning**: `IFNULL(expr1, expr2)` evaluates `expr1`. If it is `NULL`, it returns `expr2`; otherwise, it returns `expr1`.

2. **Answer: B**
   - **Reasoning**: In SQL `LIKE` patterns, `%` represents zero or more arbitrary characters, whereas `_` matches exactly one character.
