# Module 03 Quiz

1. Which MySQL function takes two arguments and returns the second argument if the first is `NULL`?
   - A) `NULLIF()`
   - B) `IFNULL()`
   - C) `CONCAT()`
   - D) `ISNULL()`

2. What wildcard character in `LIKE` matches any sequence of zero or more characters?
   - A) `_`
   - B) `%`
   - C) `*`
   - D) `?`
