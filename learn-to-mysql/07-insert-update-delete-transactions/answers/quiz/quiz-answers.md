# Module 07 Quiz Answers

1. **Answer: C**
   - **Reasoning**: `TRUNCATE TABLE` is a DDL operation that re-initializes the table, resetting `AUTO_INCREMENT` sequence numbers and bypassing row-level delete triggers.

2. **Answer: B**
   - **Reasoning**: MySQL supports `INSERT INTO table SET col1 = val1, col2 = val2;` as an alternative single-row insert syntax.
