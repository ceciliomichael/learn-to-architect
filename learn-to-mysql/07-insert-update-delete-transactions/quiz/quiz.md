# Module 07 Quiz

1. Which command empties a table completely, resets its `AUTO_INCREMENT` sequence, and cannot be rolled back in a transaction?
   - A) `DELETE FROM table;`
   - B) `REMOVE TABLE;`
   - C) `TRUNCATE TABLE;`
   - D) `DROP TABLE;`

2. What does `INSERT INTO users SET username = 'sam';` do in MySQL?
   - A) Throws a syntax error because `SET` is reserved for `UPDATE`.
   - B) Inserts a new row specifying columns via assignment syntax.
   - C) Updates existing user 'sam'.
   - D) Creates a session variable named 'sam'.
