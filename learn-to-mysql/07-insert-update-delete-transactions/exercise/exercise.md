# Module 07 Exercise

1. Insert 2 rows into `users` using `INSERT INTO ... SET` syntax.
2. Perform a preview `SELECT` and `UPDATE` role to `'superadmin'` for username `'alice'`.
3. Explain why `TRUNCATE TABLE` resets auto-increment IDs.
